using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum Statements
{
   None,
   IncompleteTray,
   CompleteTray,
   Intro,
   Cube, 
   Sphere, 
   Cone,
   Packaging,
   NextSteps,
   ObjectAdded
}
